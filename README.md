# qa-ableton-mcp-schemas

Shared Zod schemas + types for the qa-ableton-mcp wire format (kernel serializers ↔ MCP outputSchema).

Part of [QuantumAudio](https://github.com/QaAudio). License: Apache-2.0.

## Development

```bash
npm ci
npm run typecheck
npm run build
```
